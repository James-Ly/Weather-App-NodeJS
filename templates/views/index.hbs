<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.6.2/css/bulma.css" />
</head>

<body>
    <section class="hero is-primary">
        <div class="hero-body">
            <div class="container">
                <h1 class="title">
                    What's the weather like?
                </h1>
            </div>
        </div>
    </section>
    <section class="section">
        <div class="container">
            <div class="columns">
                <div class="column is-offset-4 is-4">
                    <form method="POST" action="/">
                        <div class="field has-addons">
                            <div class="control is-expanded">
                                <input type='text' name='name' placeholder='City Name' class='input' maxlength='25'
                                    required id='id_name'>
                            </div>
                            <div class="control">
                                <input class="button is-info" type="submit" autocomplete='off' value='Add City'>
                            </div>
                        </div>
                        {{!-- {% if message %}
                        <div class="notification {{ message_class }}">{{ message }}</div>
                    {% endif %} --}}
                    </form>
                </div>
            </div>
        </div>
    </section>
    <section class="section">

        <div class="container">
            <div class="columns">
                <div class="column is-offset-4 is-4">
                    {{#each cities}}
                    <div class="box">
                        <article class="media">
                            <div class="media-left">
                                <figure class="image is-50x50">
                                    <img src="http://openweathermap.org/img/w/{{ icon }}.png" alt="Image">
                                </figure>
                            </div>
                            <div class="media-content">
                                <div class="content">

                                    <p>
                                        <span class="title">{{ city }}</span>
                                        <br>
                                        <span class="subtitle">{{ temperature }}° C</span>
                                        <br> {{ description }}
                                    </p>
                                </div>
                            </div>
                            <div class="media-right">
                                <form method="POST" action='delete_city/{{city}}'>
                                    <a>
                                        <input type='submit' class="delete">
                                    </a>
                                </form>
                            </div>
                        </article>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>

    </section>
    <footer class="footer">
    </footer>
</body>

</html>